import React from 'react'

const Headers = () => (
    <header>
        <img alt='logo'></img>
        <input type='text' placeholder='O que está procurando?'/>
    </header>
);

export default Headers;